# =====================
# Docker Compose .env example
# Copy this file to `.env` and fill real secrets.
# Do NOT commit your `.env` file.
# =====================

# MySQL
MYSQL_ROOT_PASSWORD=changeme
MYSQL_DATABASE=appdb

# Spring datasource (used by AuthService & InventoryService)
SPRING_DATASOURCE_USERNAME=root
SPRING_DATASOURCE_PASSWORD=changeme

# JWT Secret (use a long random string or base64)
JWT_SECRET=change_this_secret_to_long_random

# Cloudinary (optional, required by InventoryService if using image upload)
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Timezone for containers
TZ=Asia/Ho_Chi_Minh
